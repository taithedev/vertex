# Vertex Desktop

A Tauri 2 launcher foundation for the Vertex platform.

The desktop client is intentionally separate from the web runtime. Future milestones can add authenticated launcher views, installed-game tracking, signed game updates, and a safe custom-protocol flow that validates game IDs against the configured Vertex origin.

The web app remains the source of truth for authentication and platform data.
